# abc-website 轉換系統 — 介面契約 v1.2（🩹 止血 final，覆蓋 ~80% 場景，剩 7 洞列 v1.3 backlog）

> 🩹 **v1.2 狀態：止血 final，不是根治**。v1.1 4 致命洞修法落地（覆蓋「在這台 Mac、用 git CLI、不下 --no-verify」場景），但**第三輪 yi-challenge 揭露 7 個剩餘可中招 path 沒解**（見 §11.7 v1.3 backlog）。
>
> **接受止血定位的理由**：剩餘 7 洞影響的是「下次改字典 / 跨機器 / 用 web UI / 跨 repo 部署原子性」場景，對一休「單機 + AI 都在這台 Mac + git CLI」的實際工作模式邊際收益遞減；但義務必須盡（標 🩹 + 列剩餘 path + v1.3 deadline，Pattern 9）。
>
> **v1.2 4 致命洞修法**（部分解 / 真解）：
>
> 1. ⚠️ §10 鐵律 → abc-website `.git/hooks/commit-msg` 強制 commit msg 含 `baseline / 假說 / GO`（漏斗檔案 trigger）— **只擋這台 Mac 的 git CLI 路徑**
> 2. ⚠️ §11 觸發條件鏈死 → 雙觸發（主觸發 + 🗓 review-by 2026-06-11）— **純文字無自動 reminder**
> 3. ⚠️ §5.2 跨 repo 字典原子性 → 雙端 hook 強制 commit msg 含對端 hash — **commit-time 解，push-time 部署窗口仍撕裂**
> 4. ✅ §3.2.1 quiz 命名衝突 → `click_line_cta_bottom` 改名 `quiz_click_line_cta_bottom`（真解）
>
> **每次開新對話前讀這份。** abc-website 的 7 個工具、漏斗、事件、Supabase 表、CTA、UTM、LINE 端對接 — 所有「已散落但沒一份完整紀錄」的東西都在這份契約裡。
>
> **改動規則（不可跳過）**：
>
> - 改任一工具的事件命名 / 欄位 / 文案 → 同步更新此契約
> - 兩處 GAP_LINES 字典維護 → 兩 repo 一起改（abc-website + official-yihugh-line-bot）+ commit msg 互引 hash，見 §5
> - 新工具上線 → 加進 §1 漏斗總圖 + §3 事件字典 + §6 Supabase 表
>
> **Git hook enforcement**（本機 `.git/hooks/`，跳過 = `--no-verify`）：
>
> - abc-website: `pre-commit`（GAP_LINES 變動警示）+ `commit-msg`（§10 三關鍵字 + §5.2 official-bot:hash 雙檢核）
> - official-yihugh-line-bot: 對稱 hook（fatty-liver 字典變動 → abc-website:hash）
> - 偵測機制：`git diff --cached -W` whole function/block context + grep（排除 `@@` hunk header 避免 false positive）
>
> **歷史**：
>
> - v1 (2026-04-29) — fatty-liver 拿掉進階上線後產出，第一輪 yi-challenge → 5 致命洞 + 4 實際錯誤
> - v1.1 (2026-04-30) — 修了 9 處（432 → 534 行）。第二輪 yi-challenge → 4 致命洞（修錯方向，文件化但無 enforcement）
> - **v1.2 (2026-04-30 同日)** — 4 致命洞修完 + 建雙端 git hook（從心理安慰升級到自動化 enforcement）

---

## 🚨 v1 必讀

**abc-website 跟 LINE Bot 契約有什麼不同**：
- 沒有 AI 輸出契約（純前端 + 靜態 HTML，無 LLM）
- 沒有狀態機（SSG 不存用戶 session，狀態在 React useState 內）
- 「狀態」改成「漏斗階段」（看結果→進階→領代碼→點 LINE）
- 「下游消費者」是 official-yihugh-line-bot（claim_code 領取後產報告）

**動手前必查**：
- 改 `_sessions` 表 schema → 先看 §6 該表 + §9 LINE 端對應
- 改個人化文案字典 → 看 §5 兩處同步規則
- 改事件命名 → 看 §3 命名規則 + §10 已知不一致清單

---

## 目錄

1. [漏斗總圖（7 工具 × 4 階段）](#1-漏斗總圖)
2. [工具清單與設計差異](#2-工具清單與設計差異)
3. [GA4 事件字典](#3-ga4-事件字典)
4. [CTA 分布表](#4-cta-分布表)
5. [個人化文案規則](#5-個人化文案規則)
6. [Supabase Schema（5 個 sessions 表）](#6-supabase-schema)
7. [UTM 命名規範](#7-utm-命名規範)
8. [見證與測驗結果頁](#8-見證與測驗結果頁)
9. [LINE 端對應](#9-line-端對應)
10. [設計決策史](#10-設計決策史)
11. [已知缺口與待辦](#11-已知缺口與待辦)

---

## 1. 漏斗總圖

每個工具的標準漏斗有 4 階段（除非該工具沒 claim 流程）：

```
看結果（complete）
  ↓ 願意填進階？
進階（start_advanced）
  ↓ 完成進階？
領代碼（claim_generated）— 寫 Supabase + 顯示代碼
  ↓ 點 LINE？
點 LINE（click_line_<tool>）— 帶代碼到 LINE 領完整報告
```

> **fatty-liver 例外（2026-04-29 起）**：沒進階，看結果直接 await `generateClaim` → claim_generated 跟 complete 幾乎同時發生

每個工具的階段覆蓋見 §3 事件矩陣。

---

## 2. 工具清單與設計差異

| 路由 | 工具 | 表單型態 | 進階？ | claim_code 流程？ | 主推路徑 |
|------|------|---|:-:|:-:|---|
| `/tools` | TDEE / BMR | 計算型（gender + 年齡身高體重 + 活動量） | ✅ 3 題進階（diet_method / breakfast_habit / afternoon_craving） | ✅ | 拿代碼 + quiz 借力 |
| `/tools/protein` | 蛋白質 | 計算型 | ✅ 3 題進階（breakfast / lunch / dinner_type） | ✅ | **quiz 借力為主**（拿代碼 3.3% vs quiz 6.6%）|
| `/tools/waist-hip` | 腰臀比 | 計算型（腰圍 + 臀圍） | ❌ 無 | ❌ 無代碼流程 | LINE generic CTA（「之後開放體驗時優先通知」）|
| `/tools/blood-sugar` | 血糖穩定度 | 風險評估（症狀勾選） | ✅ 2 題進階（drink_habit / lunch_habit） | ✅ | 拿代碼路 |
| `/tools/fatty-liver` | 脂肪肝風險 | 風險評估（6 題 0-3 分） | 🟫 **2026-04-29 拿掉**（看結果直接 claim） | ✅ | 拿代碼路（最強 13.8%） |
| `/tools/insulin-check` | 胰島素阻抗 | 風險評估 | ❌ 無 | ❌ 無代碼流程 | LINE generic CTA |
| `/tools/sugar` | 每日糖攝取 | 計算型（體重 + 杯數 + 甜度） | ✅ 進階（drink_time / topping） | ✅ | 拿代碼路 |

### 設計差異原則

| 工具型態 | 該不該保留進階？ | 原因 |
|---|---|---|
| 計算型（蛋白質 / 糖） | ⚠️ 待測 | 計算型工具承諾感低，可能適合 quiz 借力 |
| 風險評估型（脂肪肝 / 血糖 / 胰島素） | ⚠️ 待測 | 已有風險警示鉤住用戶，承諾感高 → 拿代碼路效率好 |
| TDEE | ⚠️ 混合 | 計算型但量大，目前進階保留觀察 |

> 04-29 fatty-liver 拿掉進階是 v1 假說「風險評估型工具用 6 題 answers[] 直接展開個人化即可，進階兩題反而是阻力」的試驗。1-2 週後若數字提升，sugar / blood-sugar / protein 比照辦理（暫時不動 TDEE，量太大改動風險高）

---

## 3. GA4 事件字典

### 3.1 工具漏斗事件命名規則

| 階段 | 命名 pattern | 例子 |
|------|---|---|
| 看結果 | `<tool>_complete` | `tdee_complete` / `sugar_complete` / `fatty_liver_complete` |
| 進階 | `<tool>_start_advanced` | `protein_start_advanced` |
| 領代碼 | `<tool>_claim_generated` | `blood_sugar_claim_generated` |
| 點 LINE | `click_line_<tool>` | `click_line_fatty_liver` |

### 3.2 7 工具事件矩陣

| 工具 | complete | start_advanced | claim_generated | click_line_X |
|------|---|---|---|---|
| TDEE | `tdee_complete` | `tdee_start_advanced` | `tdee_claim_generated` | `click_line_tdee` |
| 蛋白質 | ⚠️ `protein_calculate`（命名歷史包袱，語意 = complete）| `protein_start_advanced` | ⚠️ `protein_complete`（同名複用為 claim）| `click_line_protein` |
| 腰臀比 | ❌ 無事件 | — | — | ⚠️ generic `click_line_cta` + source=`waist_hip` |
| 血糖穩定度 | `blood_sugar_complete` | `blood_sugar_start_advanced` | `blood_sugar_claim_generated` | `click_line_blood_sugar` |
| 脂肪肝 | `fatty_liver_complete` | 🟫 dead（04-29 起）| `fatty_liver_claim_generated`（看結果直接觸發）| `click_line_fatty_liver` |
| 胰島素阻抗 | `insulin_check_complete` | — 無代碼流程 | — | ⚠️ generic `click_line_cta` + source=`insulin_check` |
| 每日糖攝取 | `sugar_complete` | `sugar_start_advanced` | `sugar_claim_generated` | `click_line_sugar` |

### 3.2.1 測驗結果頁（`/quiz` 靜態 HTML）獨立事件

`public/quiz/index.html` 是獨立靜態 HTML（不走 Next.js page），有自己的事件命名：

| 事件 | 觸發位置 | 參數 |
|------|---|---|
| `quiz_start` / `quiz_complete` | 測驗開始 / 完成 | `metabolism_type` |
| `click_line_deeplink` | 結果頁主 LINE 一鍵領取 | `metabolism_type` |
| `click_line_yixiu` | 加一休本人 LINE 連結 | `metabolism_type` |
| `click_line_report` | 「在 LINE 領完整報告」次按鈕 | `metabolism_type` |
| `quiz_click_line_cta_bottom` | 結果頁底部 LINE CTA（v1.2 從 `click_line_cta_bottom` 改名以避開跟工具頁同名）| `metabolism_type, has_code` |
| `click_learning_path` | 學習路徑 3 步驟 | `metabolism_type, step, label` |
| `click_browse_articles` | 瀏覽文章按鈕 | `metabolism_type` |
| `click_content_link` | 結果頁文章連結 | `metabolism_type, slug` |
| `click_story_link` | 學員見證連結 | `metabolism_type` |
| `share` | 分享按鈕 | `metabolism_type, channel` |

> ✅ **v1.2 已解**：quiz 端事件改名為 `quiz_click_line_cta_bottom`（前綴 `quiz_`），跟工具頁 `click_line_cta_bottom` 不再同名。歷史 GA4 資料保留舊名供 04-30 前對照

### 3.3 事件參數值域

| 事件 | 參數 | 值域 |
|------|---|---|
| `*_complete` | risk / score / 工具特有 | risk: `low` / `moderate` / `high`（風險評估型）/ score: number |
| `*_claim_generated` | risk / score / 工具特有 | 同上 |
| `click_line_<tool>` | code, risk | code = 4 字英數，risk 同上 |
| `click_quiz_cta` | source | enum: `protein` / `protein_main` / `protein_bottom` / `tdee` / `tdee_bottom` / `waist_hip` / `waist_hip_bottom` / `insulin_check` / `insulin_check_bottom` / `fatty_liver` / `fatty_liver_bottom` / `sugar` / `sugar_bottom` / `blood_sugar` / `blood_sugar_bottom` |
| `click_line_cta` / `click_line_cta_bottom` | source | 同 click_quiz_cta |
| `click_sticky_line` | page | 當前 pagePath |
| `click_line_deeplink` | metabolism_type | quiz 五種代謝類型 enum |
| `quiz_start` / `quiz_complete` | metabolism_type | 同上 |

### 3.4 已知不一致（Pattern 8 待清）

- **protein 命名歷史包袱**：`protein_calculate` 是「看結果」、`protein_complete` 是「拿代碼」 → 跟其他工具命名 swap。要改的話 GA4 探索報表要分段對照（觸發條件：下次大版本改 protein 漏斗時順便處理）
- **waist_hip 缺 complete 事件**：算出結果時沒 fire `waist_hip_complete`，無法看「多少人完成計算」（觸發條件：下次改 waist_hip 工具時補上）
- **waist_hip / insulin_check 用 generic `click_line_cta` + source**：**這個是有意設計**（CLAUDE.md line 66）。**判斷準則**：
  - 工具有 claim_code 領取流程 → 用專屬事件 `click_line_<tool>`（精準分流到該工具的漏斗轉換率）
  - 工具無 claim_code 流程（純資訊型）→ 用 generic `click_line_cta` + `source` enum（用 GA4 dimension 區分來源）
  - 未來加新工具時依此準則決定，避免「為了一致而收斂」破壞既有 GA4 funnel 精準度

---

## 4. CTA 分布表

### 4.1 全站常駐 CTA

| 元件 | 出現位置 | 觸發事件 | 文案 |
|------|---|---|---|
| StickyLineCTA | 全站 20 頁，滾動 600px 後出現，接近底部隱藏 | `click_sticky_line` | 「加 LINE 免費領你的代謝報告」（2026-04-23 後鉤子文案：「你是哪種代謝型？」）|

### 4.2 工具頁 CTA 階層

```
結果區（risk badge / 數字）
  ↓
The Gap（個人化打臉文案，用 answers[]）— fatty-liver / blood-sugar / sugar 等風險型工具
  ↓
拿代碼按鈕（fatty-liver 起為「領取你的個人化XX建議 →」直接觸發 generateClaim；其他工具仍是「告訴你我怎麼吃 →」開進階）
  ↓
拿到代碼後：個人化診斷預覽（模糊區）+ LINE 領取按鈕
  ↓
測驗 CTA（次級）：「30 秒測出你的代謝類型 →」
  ↓
LINE 副 CTA：「加入一休的 LINE 之後...」
  ↓
SEO 內容
  ↓
底部收尾 CTA
```

### 4.3 測驗結果頁 CTA（`/quiz`）

社會證明（1,200+） + 隨機見證卡片（5 類型 × 3 人）+ 學習路徑（3 步驟）+ 科學深入頁 + LINE deeplink

---

## 5. 個人化文案規則

### 5.1 文案資料來源

每個工具的個人化文案有兩層：

| 層 | 在哪 | 用什麼資料 |
|---|---|---|
| **網站端**（用戶看結果時看到） | abc-website 工具 page.tsx | 用 React state（answers / drinkHabit / 等）動態組合 |
| **LINE 端**（用戶領完代碼後收到） | official-yihugh-line-bot route.js | 從 Supabase `_sessions` 表撈 row → 字典 lookup |

### 5.2 兩端必須對應的字典

> ⚠️ **動手紅線**：改任一邊字典必須同步改另一邊，否則網站承諾跟 LINE 兌現會不對齊。

| 工具 | 網站字典 | LINE 端字典 | trigger 真相來源 |
|---|---|---|---|
| **fatty-liver** | `GAP_LINES`（`src/app/tools/fatty-liver/page.tsx` L163 定義 / L216 使用）| `FATTY_LIVER_GAP_LINES`（`app/api/webhook/route.js` L2072 定義 / L2097-2098 使用）+ legacy `DRINK_AHA` / `LUNCH_AHA` + `FATTY_LIVER_RISK` | 6 題 `QUESTIONS.options.label`（label 字串就是 trigger）|
| **blood-sugar** | （網站端目前無對應 GAP_LINES，個人化文案散在 page.tsx if/else）| `BLOOD_SUGAR_DRINK_AHA` / `BLOOD_SUGAR_LUNCH_AHA` / `BLOOD_SUGAR_RISK_DATA` | `drink_habit` (4 enum) / `lunch_habit` (4 enum) / `risk_level` (4 enum: low/moderate/high/very-high) |
| **sugar** | （網站端目前無對應 GAP_LINES）| `SWEETNESS_GRAMS` (5 enum) / `CUPS_NUM` (4 enum) | `sweetness_level` / `cups_per_day` |
| **protein** | （網站端目前無對應 GAP_LINES）| `MEAL_DB`（每餐 × food_type）| `breakfast_type` / `lunch_type` / `dinner_type` / `food_type` |
| **TDEE** | （網站端目前無對應 GAP_LINES）| `BREAKFAST_PROTEIN` (4 enum) / `DIET_METHOD_AHA` (4 enum) | `breakfast_habit` / `diet_method` / `afternoon_craving` |
| 代謝測驗（quiz） | 靜態 HTML 內含類型文案 | `TYPE_DATA` (5 種代謝類型) | `metabolism_type` enum: high-rpm / burnout / roller-coaster / power-save / steady |

> **目前只有 fatty-liver 是「兩端字典對應」pattern**（網站 `GAP_LINES` ↔ LINE `FATTY_LIVER_GAP_LINES`）。其他工具的網站端文案是 if/else 散在 page.tsx，LINE 端字典獨立維護，沒有對應約束。
>
> **未來各工具改成「拿代碼直接領」pattern 時**（仿 fatty-liver），會把字典結構也對齊：網站建 `GAP_LINES` + LINE 建 `<TOOL>_GAP_LINES`，這份契約對應節要更新

### 5.2.1 Enforcement 現況（v1.2 升級）

> ✅ **v1.2 起：雙端 git hook 自動化 enforcement**（GAP_LINES 變動必須 commit msg 互引 hash）

**現有防線**（v1.2 落地）：

| 防線 | 位置 | 行為 |
|---|---|---|
| abc-website `.git/hooks/pre-commit` | 本機 hook | 偵測 GAP_LINES 變動 → 印警示「commit msg 必須含 official-bot:<hash>」（不擋）|
| abc-website `.git/hooks/commit-msg` | 本機 hook | (1) §10 鐵律：漏斗檔案 commit 必須含 `baseline / 假說 / GO` (2) §5.2：GAP_LINES 變動必須含 `official-bot:` 加 7+ 字 hex hash，缺則 reject |
| official-yihugh-line-bot `.git/hooks/pre-commit` | 本機 hook | 對稱：偵測 fatty-liver 字典變動 → 警示 commit msg 必須含 abc-website:hash |
| official-yihugh-line-bot `.git/hooks/commit-msg` | 本機 hook | 對稱：fatty-liver 字典變動 commit msg 必須含 `abc-website:<hash>`，缺則 reject |
| route.js L2052 註解 | 程式碼註解 | 「對應 abc-website ... 如有改動兩邊要同步」（人類可讀提醒）|

**Hook 偵測機制**：

- 漏斗檔案：`git diff --cached --name-only` grep `^src/app/tools/.*page\.tsx$`
- GAP_LINES 變動：`git diff --cached -W` whole function/block context | `grep -v '^@@'` (排除 hunk header false positive) | grep `GAP_LINES` (or fatty-liver 字典名 enum on official-bot 端)
- 跳過：`git commit --no-verify`（適用 typo / import / 註解 / 不影響字典邏輯的場景）

**Hook 三場景驗收**（v1.2 已測過）：

1. ✅ 改 GAP_LINES 內值 + 完整 §10 三關鍵字 + 無 official-bot:hash → reject
2. ✅ 改 GAP_LINES 內值 + 完整 §10 + 含 `official-bot:a76ffae` → pass
3. ✅ 改 page.tsx JSX（不動 GAP_LINES）+ §10 三關鍵字 + 無 hash → pass（不誤擋）

**已知失敗案例**（v1.1 之前）：

- 2026-04-29 fatty-liver 改動時要不是 yi-challenge feasibility agent 主動讀兩 repo，會漏掉 LINE 端字典更新 — v1.2 hook 落地後此 bug 會直接 reject

**Enforcement 升級路徑**（未來若需要）：

| 機制 | 防什麼 | 成本 | 觸發建立條件 |
|---|---|---|---|
| 抽出共用 npm package（私有）| 字典源頭單一化 | 半天 | 有 ≥3 個工具走「兩端對應」pattern 時值得做 |
| GitHub Actions CI check | hook 被 `--no-verify` 繞過時的最後防線 | 1 小時 | 一休觀察到 hook 被頻繁繞過時 |

**動手前自檢**（hook 之外的人類 SOP）：

1. 改字典前先 grep 對端字典確認對應
2. trigger label 用 abc-website `QUESTIONS` 6 題 `options.label` 當真相來源（**字串完全相同 含中文標點**）
3. 兩 repo commit msg 互引 hash 形成原子記錄

### 5.3 風險等級 enum（風險評估型工具共用）

| risk_level | 文案標籤 | 顏色 |
|---|---|---|
| `low` | 低風險 | 綠 `#2a9d6f` |
| `moderate` | 中等風險 | 橘 `#d97706` |
| `high` | 高風險 | 紅 `#dc2626` |

### 5.4 fatty-liver oneStep 文案 pattern（2026-04-29）

`high` 跟 `moderate` 的 LINE 端 oneStep 都套相同 pattern：「無糖茶為基底 + 偶爾搭配配料 + 茶必須無糖不額外加糖」。`low` 是檢查項目（每年腹部超音波）不套此 pattern。

未來其他工具的 oneStep 改寫時，若涉及飲料建議，**必須對齊此 pattern**（避免不同工具給用戶矛盾建議）。

---

## 6. Supabase Schema

### 6.1 5 個 sessions 表（abc-website 寫入 / official-yihugh-line-bot 讀取 + 領取後寫入 claimed_*）

> 用 Supabase MCP `information_schema.columns` 查證（2026-04-30）。N = NOT NULL / Y = NULLABLE。

#### `tdee_sessions`
| 欄位 | 型別 | NULL | 來源 / 用途 |
|---|---|:-:|---|
| `id` | text | N | 網站端產 sessionId |
| `gender` | text | N | `female` / `male` |
| `age` | integer | N | 10-100 |
| `height` | numeric | N | 100-250 公分 |
| `weight` | numeric | N | 30-300 公斤 |
| `activity_level` | text | N | `sedentary` / `light` / `moderate` / `active` / `very-active` |
| `bmr` | integer | N | Mifflin-St Jeor 算出 |
| `tdee` | integer | N | bmr × activity_factor |
| `protein_min` / `protein_max` | integer | N | 體重 × 1.0-1.6 |
| `diet_method` | text | Y | 進階 Q1 enum: `eat-less` / `exercise` / `both` / `none` |
| `breakfast_habit` | text | Y | 進階 Q2 enum: `bread` / `skip` / `cereal` / `balanced` |
| `afternoon_craving` | text | Y | 進階 Q3 enum: `must` / `sometimes` / `rarely` |
| `claim_code` | text | Y | 4 字英數，產代碼時寫入 |
| `claimed_by` | text | Y | LINE userId，領取後 LINE 端寫入 |
| `claimed_at` | timestamptz | Y | 領取時間，LINE 端寫入 |
| `created_at` | timestamptz | Y | default now() |

#### `protein_sessions`
| 欄位 | 型別 | NULL | 來源 / 用途 |
|---|---|:-:|---|
| `id` | text | N | sessionId |
| `weight` | numeric | N | — |
| `goal` | text | N | enum: `loseFat` / `buildMuscle` / `maintain` |
| `diet_type` | text | N | derived from `goal` |
| `meal_count` | text | N | derived |
| `food_type` | text | N | enum: `omnivore` / `lacto-ovo` / `vegan` |
| `protein_min` / `protein_max` | integer | N | — |
| `breakfast_type` / `lunch_type` / `dinner_type` | text | Y | 進階每餐選擇（feeds `MEAL_DB` lookup）|
| `age` / `height` | integer / numeric | Y | 用於 BMI 跟分齡提醒 |
| `claim_code` / `claimed_by` / `claimed_at` / `created_at` | 同 tdee | Y | — |

#### `blood_sugar_sessions`
| 欄位 | 型別 | NULL | 來源 / 用途 |
|---|---|:-:|---|
| `id` | text | N | sessionId |
| `symptom_count` | integer | N | 勾選症狀數量 |
| `symptoms` | jsonb | N | 症狀文字陣列 |
| `risk_level` | text | N | enum: `low` / `moderate` / `high` / `very-high`（**4 級**，比 fatty-liver 多 very-high）|
| `drink_habit` | text | Y | 進階 Q1 enum: `sugar-tea` / `juice` / `sugar-coffee` / `water` |
| `lunch_habit` | text | Y | 進階 Q2 enum: `bento` / `noodle` / `homemade` / `skip` |
| `claim_code` / `claimed_by` / `claimed_at` / `created_at` | 同 tdee | Y | — |

#### `fatty_liver_sessions`（2026-04-29 拿掉進階）
| 欄位 | 型別 | NULL | 來源 / 用途 |
|---|---|:-:|---|
| `id` | text | N | sessionId |
| `score` | integer | N | 6 題加總 0-15 |
| `max_score` | integer | N | 滿分（目前固定 15）|
| `risk_level` | text | N | enum: `low` / `moderate` / `high`（3 級）|
| `answers` | jsonb | N | 6 題 array of `{question, answer, score}` |
| `claim_code` / `claimed_by` / `claimed_at` / `created_at` | 同 tdee | Y | — |
| `drink_habit` / `lunch_habit` | text | Y | 🟫 **legacy（2026-04-29 後新代碼不寫）**，向下相容歷史 row |

#### `sugar_sessions`
| 欄位 | 型別 | NULL | 來源 / 用途 |
|---|---|:-:|---|
| `id` | text | N | sessionId |
| `weight` | numeric | N | 算每日糖上限基準 |
| `sugar_limit_10` / `sugar_limit_5` | integer | N | WHO 建議上限（10% / 5% 熱量） |
| `cups_per_day` | text | Y | 進階 enum: `0` / `1` / `2` / `3` |
| `sweetness_level` | text | Y | 進階 enum: `full` / `less` / `half` / `slight` / `none` |
| `drink_time` | text | Y | 進階問題（時段）|
| `topping` | text | Y | 進階問題（配料） |
| `lunch_habit` / `afternoon_snack` | text | Y | ⚠️ **schema 有但 page.tsx payload 沒寫入** — 可能是過去版本殘留欄位，未來確認後決定要不要清掉 |
| `claim_code` / `claimed_by` / `claimed_at` / `created_at` | 同 tdee | Y | — |

> Supabase 連線：abc-website 用 `SUPABASE_URL` + `SUPABASE_ANON_KEY` 直 fetch（無 Supabase Client）。LINE 端用 supabase-js client。

### 6.2 共用欄位

- `id`：sessionId（前綴 + base36 random，例 `td_xx` / `fl_xx`）
- `claim_code`：4 字英數隨機（A-Z 排除 0/I/O/1，數字 0-9 排除 0/1，避免肉眼混淆），24 小時內有效
- 衝突重試：寫入 409 → 重試一次（換新 sessionId + 新 claim_code）
- `claimed_by` + `claimed_at`：**LINE 端領取後寫入**（網站端不寫）

### 6.2.1 NULL 邏輯規則（避免 Pattern 6 NULL 邏輯雷）

**`claimed_by` / `claimed_at`**：

| 狀態 | 含義 | LINE 端行為 |
|---|---|---|
| `claimed_by IS NULL` | 還沒被任何 LINE 帳號領過 | 允許領取 → UPDATE 寫入 LINE userId 跟 timestamp |
| `claimed_by = <某 LINE userId>` 且符合當前用戶 | 同人重複領（網路重連等）| 直接重發報告（不寫入第二次）|
| `claimed_by = <別的 LINE userId>` | 已被別人領過 | **目前無嚴格防衛**：理論上 4 字代碼洩漏可被人搶領。實作上 LINE 端是否禁止 = 待確認（建議：直接重發 + log，不報錯，因為代碼 24h 過期短）|

**legacy 欄位 fallback 行為**（針對 fatty-liver `drink_habit` / `lunch_habit`）：

- 04-29 後新代碼**不寫入**這兩欄，但歷史 row（04-29 前）**有值**
- LINE 端 `buildFattyLiverReport` 升級後（04-29 commit `a76ffae`）**完全不讀**這兩欄，**100% 改用 `answers[]` + `FATTY_LIVER_GAP_LINES`**
- 結果：**新舊 row 一視同仁走新邏輯**。歷史 row 的 `drink_habit / lunch_habit` 變成歷史紀錄欄位（純資料保留，無讀取者）
- 例外：如果未來想做「歷史用戶 vs 新用戶 LINE 訊息品質對照分析」，可手動 SELECT 比較 — 但**一般 LINE webhook 流程不讀這兩欄**

**WHERE 子句的 NULL 陷阱**（給未來 admin SQL）：
- `WHERE drink_habit = 'sugar-tea'` → NULL row 永遠不命中（PostgreSQL `NULL = anything` 永遠是 NULL）
- `WHERE drink_habit IS DISTINCT FROM 'unknown'` → NULL row 命中（IS DISTINCT FROM 把 NULL 當值處理）
- 寫統計查詢時要明確 `IS NULL` / `IS NOT NULL` 過濾，否則新版用戶（NULL）會被靜默排除

### 6.3 schema migration 規則

- 加新欄位 → 必須 NULLABLE（向下相容歷史 row）
- 拿掉欄位 → **不要 DROP COLUMN**，標 legacy 即可（避免歷史 row 撈起來爆 + 保留歷史分析能力）
- 欄位重新命名 → 不允許（會破壞 LINE 端 lookup）

---

## 7. UTM 命名規範

> 完整版見 `abc-website/CLAUDE.md` UTM 章節。此處摘要：

| 來源 | utm_source | utm_medium | utm_campaign |
|---|---|---|---|
| abc-line-bot（阿算） | `line` | `bot` | `asuan` |
| official-yihugh-line-bot | `line` | `bot` | `official` |
| official-bot 報告類型頁連結 | `line` | `bot` | `report` |

GA 區分：`line/bot/asuan` vs `line/bot/official` vs `line/bot/report`

LINE deeplink 三 URL 拆分（2026-04-23 上線）：sticky / quiz / generic 三條 URL 做 GA4 追蹤分流。

---

## 8. 見證與測驗結果頁

### 8.1 測驗（`/quiz`）

獨立靜態 HTML（`public/quiz/index.html`），不是 Next.js page。連結用 `<a>` 不用 `<Link>`。

5 種代謝類型 enum：`high-rpm` / `burnout` / `roller-coaster` / `power-save` / `steady`

### 8.2 見證卡片

5 類型 × 3 人 = 15 個化名見證，隨機顯示。
- 人設對齊受眾：12 女 3 男，年齡 35-47 歲，混合職業
- 受眾數據來源：LINE 好友統計（85% 女性、核心 40-44 歲）

---

## 9. LINE 端對應

### 9.1 哪個 Bot 接哪個工具

| 來源 | 接收 Bot | 處理位置 |
|---|---|---|
| abcmetabolic.com 全部 5 個 sessions 工具的 claim_code | **official-yihugh-line-bot** | route.js **L490 `handleCodeClaim`**（其下 L505-555 各 `_sessions` 表 lookup / L587-775 報告產出）|
| abc-line-bot（阿算）| **不接 sessions 表** | 阿算自己有獨立漏斗 |

### 9.2 LINE 端報告產生器（official-bot route.js）

| 工具 | 產生器函式 | 字典依賴 |
|---|---|---|
| 代謝測驗 | `buildPersonalizedReport` | `TYPE_DATA`（5 種代謝類型）|
| TDEE | `buildTdeeReport` | `BREAKFAST_PROTEIN` (4) / `DIET_METHOD_AHA` (4) |
| 蛋白質 | `buildProteinStrategy`（fallback `buildProteinStrategyLegacy`）| `MEAL_DB`（每餐 × food_type）|
| 血糖穩定度 | `buildBloodSugarReport` | `BLOOD_SUGAR_DRINK_AHA` (4，含 water=null) / `BLOOD_SUGAR_LUNCH_AHA` (4，含 homemade=null) / `BLOOD_SUGAR_RISK_DATA` (4 級) |
| 脂肪肝 | `buildFattyLiverReport` | `FATTY_LIVER_GAP_LINES` (7) + `FATTY_LIVER_RISK` (3 級) + legacy `DRINK_AHA` (4，含 water=null) / `LUNCH_AHA` (4，含 homemade=null) |
| 每日糖攝取 | `buildSugarReport` | `SWEETNESS_GRAMS` (5) / `CUPS_NUM` (4) |

> 命名不一致：5 個工具用 `buildXReport`，protein 用 `buildXStrategy`（命名歷史包袱）。未來重構時可以收斂。

### 9.3 測驗結果頁的 LINE deeplink

不走 sessions 表，走 metabolism_type 直接 deeplink。事件 `click_line_deeplink` 帶 metabolism_type。

---

## 10. 設計決策史

### 2026-03-22
33 頁 SSG 上線，建立基本工具頁架構

### 2026-04-03
全站 CTA 架構定版：StickyLineCTA + 工具頁 CTA 模式（結果區 → 痛點 → 測驗主 CTA → LINE 副 CTA → 輔助 → SEO）

### 2026-04-07
fatty-liver / blood-sugar / sugar 工具首版上線（**無進階問題**），結果頁直接 LINE 領取

### 2026-04-XX（commit `d92401b`）
TDEE / 脂肪肝統一升級為「二段式 + 毛玻璃 + 真實內容 blur」，加進階兩題。**無數據驅動**（純直覺套二段式 pattern）

### 2026-04-23
LINE CTA 分三 URL（sticky / quiz / generic）做 GA4 追蹤分流 + StickyLineCTA 鉤子文案換成「你是哪種代謝型？」

### 2026-04-25
sugar 工具導航在全站工具頁 / footer / sitemap 補洞

### 2026-04-29
fatty-liver **拿掉進階兩題**，看結果直接 await `generateClaim`。網站承諾「N 個對你的肝最危險的習慣」，LINE 端 `buildFattyLiverReport` 用 `answers[]` + `FATTY_LIVER_GAP_LINES` 展開兌現（commit abc-website `e1e4db7` + official-bot `a76ffae`）

### 2026-04-30
契約 v1 → v1.1 → v1.2 三輪修正同日完成：

- **v1**：第一份系統契約（432 行）
- **v1.1**：第一輪 yi-challenge 抓 5 致命洞 + 4 錯誤 → 修 9 處（534 行）
- **v1.2**：第二輪 yi-challenge 揭露 v1.1 修錯方向（「文件化但無 enforcement」）→ 4 致命洞修法落地：
  - abc-website + official-yihugh-line-bot 雙端 git hook（pre-commit + commit-msg）
  - §10 三關鍵字 + §5.2 跨 repo hash 互引自動 enforce
  - §11 雙觸發機制（主觸發 + 🗓 review-by 2026-06-11 死線）
  - quiz `click_line_cta_bottom` → `quiz_click_line_cta_bottom` 解命名衝突
- **核心教訓**：契約若沒 enforcement = 心理安慰。文件 + hook 雙軌才有效

---

### 🚨 從歷史提取的鐵律（不可違反）

**04-07 → 04-XX → 04-29 三段式歷史證明**：abc-website 過去發生過「無進階 → 加進階 → 拿掉進階」的迴圈，其中 d92401b（加進階）commit message 沒提任何指標，是「直覺套二段式 pattern」。

**為避免再犯，加進階 / 改漏斗階段 / 改 CTA 主推路徑前必須有**：

1. **Baseline 數據**：當前該工具的 `*_complete` → `click_line_<tool>` 全段轉換率（至少 100 人 / 7 天樣本）
2. **假說**：明確寫出「改完後預期哪段轉換率提升幾個 % / 為什麼」
3. **GO/NO-GO 線**：「達到 X% 視為成功 / 跌破 Y% 必須回滾」
4. **觀察期 deadline**：1-2 週為單位

**檢核方式**（v1.2 起自動化）：

- ✅ **commit-msg hook 強制執行**：動到 `src/app/tools/*/page.tsx` 任一檔，commit msg 必須含 `baseline` + `假說` + `GO` 三關鍵字（任一缺則 reject，見 §5.2.1）
- 範例（04-29 fatty-liver `e1e4db7` pattern）：
  ```
  baseline: fatty_liver_complete → click_line_fatty_liver = 13.8%
  假說: 拿掉進階 → 預期 complete→claim 從 31% 降到 14-20%
  GO/NO-GO: 觀察 1-2 週, 全段轉換率 ≥ 13.8% baseline 才推進其他工具
  ```
- 跳過：`git commit --no-verify`（適用純文案改字 / typo / 重構不動漏斗）

---

## 11. 已知缺口與待辦

> ⚠️ **觸發機制設計（v1.2）**：v1.1 的單一觸發（「下次改該工具時順便處理」）有鏈死風險（fatty-liver 觀察期不通過 → 整條 backlog 卡死）。v1.2 起每條 TODO 都有**雙觸發**：
>
> - **(a) 主觸發** — 自然發生時順便處理（事件驅動）
> - **(b) 死線觸發** — 無條件 review-by 日期。到日子強制三選一：✅ 做 / ❌ 不做 / ⏸ 延後 N 週（必須記錄理由 + 新 review-by）
>
> **死線到了 = review，不是必做**。Review 結果寫入此章節對應 TODO。

### 11.1 數據觀察期（2026-05-01 ~ 05-12）

- baseline `fatty_liver_complete` → `click_line_fatty_liver` 全段轉換率 = 13.8%
- 觀察「拿掉進階」對絕對 LINE 點擊人數的影響
- adversarial 警示：31% claim→LINE 是建立在「填過進階高承諾感」的 35 人上，拿掉後可能稀釋到 14-20%。最差 case = 持平不輸不贏
- 🗓 **review-by 2026-05-12**（觀察期結束日）— 結論寫入下方：
  - [ ] 通過（≥ 13.8% baseline 且絕對 LINE 點擊提升）→ 觸發 §11.2 排程
  - [ ] 不通過 / 持平 → 凍結 §11.2 工具一致化計畫，6 週後（2026-06-23）re-review 是否延長觀察 / 改方向 / 放棄
  - [ ] inconclusive（樣本不足）→ 延長 1-2 週

### 11.2 工具一致化（觀察期後若數字 OK）

**主觸發**：§11.1 觀察期結果 — fatty-liver 全段轉換率 ≥ 13.8% baseline 且絕對 LINE 點擊人數提升
**死線觸發**：🗓 **review-by 2026-06-11**（無論觀察期結果，到日子三選一）

按優先序處理（每個都要按 §10 鐵律附 baseline + 假說 + GO/NO-GO，commit-msg hook 會擋）：

1. **blood_sugar**
   - 主觸發：fatty-liver 觀察期通過
   - 動作：比照拿掉進階 + 升級 LINE 端用 `answers[]` 展開（要先補網站端 `BLOOD_SUGAR_GAP_LINES` 字典）
   - 🗓 review-by 2026-06-11

2. **sugar**
   - 主觸發：blood_sugar 觀察期通過
   - 動作：同上
   - 🗓 review-by 2026-06-11（如 blood_sugar 還沒做完，這條順延，但仍要記錄延後理由）

3. **protein**（**不在優先序**）
   - 量大且 quiz 借力路本來就贏（6.6% vs 拿代碼 3.3%），拿掉進階會打亂 quiz 路
   - 🗓 review-by 2026-06-11（review 內容：是否仍維持「不做」決策，或有新數據改變判斷）

4. **TDEE**（**不在優先序**）
   - 量最大、進階已是 NULLABLE 無強制，改動風險不對稱
   - 🗓 review-by 2026-06-11（同上）

### 11.3 字典盤點（§5 TBD 處）

**主觸發**：「下次改該工具漏斗或新增字典 pattern 前必須先盤點」
**死線觸發**：🗓 **review-by 2026-06-11**

盤點任務 owner = 該次改動的 AI：
- **sugar** 個人化文案在哪（網站 + LINE 端各自字典）— 主觸發：進入 §11.2 排程時 / 死線：2026-06-11
- **protein** 同上 — 主觸發：未來若改 protein 漏斗時 / 死線：2026-06-11
- **TDEE** 字典已知部分（BREAKFAST_PROTEIN / DIET_METHOD_AHA）但網站端對應字典待盤點 — 主觸發：未來若改 TDEE 漏斗時 / 死線：2026-06-11

### 11.4 命名一致化（Pattern 8）

**主觸發**：「下次大版本改動該工具時順便處理（不為命名專門開 PR）」
**死線觸發**：🗓 **review-by 2026-06-11**

- `protein_calculate` → `protein_complete` 命名 swap — 主觸發：下次改 protein 漏斗時。成本：歷史 GA4 報表分段對照。死線：2026-06-11
- `waist_hip_complete` 補事件 — 主觸發：下次改 waist_hip 工具時。死線：2026-06-11
- 5 個 `click_line_<tool>` vs `click_line_cta + source` — **保留現狀**（見 §3.4 修正後判斷準則）。**不刪除選項**，未來若加新工具時可依準則再決定要不要收斂

### 11.5 schema 殘留欄位（sugar_sessions）

- `sugar_sessions.lunch_habit` / `afternoon_snack` schema 有但 page.tsx payload 沒寫入
- 主觸發：下次改 sugar 漏斗時順便清掉（標 legacy 不 DROP COLUMN，見 §6.3）
- 🗓 review-by 2026-06-11

### 11.6 領取防衛（§6.2.1 brute-force 風險）

- 4 字代碼 + 無 rate limit + 24h 過期，理論上洩漏可被人搶領
- 主觸發：發生實際被搶領事件 / 或 §11.2 三條工具一致化都做完後
- 🗓 review-by 2026-06-11（review 是否仍接受此風險，或加 rate limit / 延長代碼長度）

### 11.7 v1.3 enforcement 升級 backlog（🩹 v1.2 止血未解的 7 洞）

> 第三輪 yi-challenge 揭露：v1.2 hook 只擋「在這台 Mac、用 git CLI、不下 --no-verify」這條 path。剩 7 個可中招場景需 v1.3 解。

**Pattern 9 強制義務**：v1.2 是止血不是根治。下列 7 洞每個都標主觸發 + 死線。

| # | 洞 | v1.3 修法 | 主觸發 | 死線 |
|---|---|---|---|---|
| 1 | 🔴 hook 不在 git tracked（新 clone / 換機沒 hook）| `scripts/install-hooks.sh` + `package.json postinstall` 套到所有 clone | 換新 Mac / 加 dev / 任何新 clone 行為 | 🗓 review-by 2026-06-11 |
| 2 | 🔴 GitHub web UI commit 完全繞 hook → 直接打 Vercel prod | GitHub Actions required check（PR gate）| 觀察到自己用過 web UI commit / 加 dev 協作 | 🗓 review-by 2026-06-11 |
| 3 | 🔴 跨 repo push 順序部署窗口（30s-分鐘級線上字典撕裂）| npm package / git submodule 抽 GAP_LINES 字典單一真相來源 | 第二個工具加入字典對應 pattern（§11.2 觀察期通過）| 🗓 review-by 2026-06-11 |
| 4 | ⚠️ hash 可造假（`official-bot:0000001` 也過）| hook 跑 `git ls-remote <對端>` 驗 hash 真實存在 | 發生實際造假事故 / 跨 repo 字典變多 | 🗓 review-by 2026-06-11 |
| 5 | ⚠️ §10 baseline 只驗格式不驗語意（可寫 `baseline: TBD` 也過）| 接受設計（人為驗證為主）+ commit 後 PR self-review checklist | 觀察到自己用 TBD 偷雞 | 🗓 review-by 2026-06-11 |
| 6 | ⚠️ §11 8 條 TODO 同日 06-11 死線 + 無自動 reminder | launchd job / Calendar event / task-signal.json 在 06-11 推提醒 | 06-11 接近時 / 觀察到漏 review | 🗓 review-by 2026-06-04（提前 1 週設提醒）|
| 7 | ⚠️ `--no-verify` / `commit-tree` / rebase 易繞 | GitHub Actions required check（同 #2）| 觀察到自己頻繁繞過 | 🗓 review-by 2026-06-11 |

**剩餘可中招 path 完整列表**（Pattern 9 義務）：

- ❌ 新 clone / 換機 / 別人機器（hook 不在 tracked）
- ❌ GitHub web UI commit（不走 local hook）
- ❌ `git commit --no-verify` / `git commit-tree` 直造 / rebase --no-verify
- ❌ 兩 repo 都 commit 但只 push 一邊
- ❌ Vercel 部署窗口期（abc-website 部署完 + official-bot 還在 build = 30s-分鐘級線上字典撕裂）
- ❌ hash 可造假（hook 不驗 hash 真存在）
- ❌ §11 8 條死線同日（review fatigue）+ 無自動 reminder

**v1.3 觸發條件**（任一命中啟動）：

- 一休觀察到自己/AI 頻繁繞過 hook（用 web UI / --no-verify）
- 加進第二個跨 repo 字典 pattern（blood_sugar / sugar 進入 §11.2 一致化）
- 跨機器協作需求出現
- 2026-06-11 死線到了強制 review

### 11.8 schema 確認

✅ **已查證（2026-04-30 用 Supabase MCP）**。§6 已更新為實際 schema。

**查證時新發現**：
1. **5 個表都有 `claimed_by` + `claimed_at`** — LINE 端領取後寫入，網站端 payload 沒寫這兩欄（這是設計，網站只負責寫入時拿 claim_code）
2. **`blood_sugar_sessions.risk_level` 是 4 級**（low/moderate/high/very-high）— 比 fatty-liver 的 3 級多一個
3. **`sugar_sessions` 有 `lunch_habit` + `afternoon_snack` 欄位**但 page.tsx payload 沒寫入 — 可能是過去版本殘留欄位。**待釐清**：要清掉還是要補寫入？
4. **`protein_sessions` 沒有 `gender` 欄位**（跟 TDEE 不同）— 蛋白質計算用 `food_type` 而不是 gender 區分
5. **TDEE 進階三題（diet_method / breakfast_habit / afternoon_craving）都是 NULLABLE** — 表示 schema 設計上 TDEE 進階就是可選（用戶可不填進階直接拿代碼），跟 fatty-liver 拿掉進階前的「強制進階」設計不同。**這意味 TDEE 比 fatty-liver 早一步具備「拿掉進階」的能力**，未來重構成本最低

---

## 維護規則

### 何時更新此契約

- ✅ 加新工具 / 改現有工具的事件命名 / 改 Supabase schema / 拿掉進階 / 加 / 改字典 / 改 CTA 階層
- ✅ 動手前先 grep 此契約看現狀
- ✅ 改完同步更新對應章節 + §10 設計決策史

### 跟 CLAUDE.md 的關係

- CLAUDE.md 是「Bot 開發框架手冊 + 設計規則 + 內容上線檢查」（給 AI 的快速 onboarding）
- 此契約是「事件 / 漏斗 / Schema / 字典 / 跨系統介面」的真相來源（給 AI 改動前查證用）
- 兩份重複時以**此契約為準**

---

*v1 由 2026-04-29 fatty-liver 拿掉進階改動觸發產出。abc-website 第一份系統契約，仿 `official-yihugh-line-bot/契約_對話路徑.md` / `契約_Q5轉換漏斗.md` pattern。*
